class calc{

  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  mult() {
  	return this.x*this.y;
    //linha a mais
  }
  
  somar() {
    return this.x+this.y;
  }
  subtrai() {
    return this.x-this.y;
  }
}